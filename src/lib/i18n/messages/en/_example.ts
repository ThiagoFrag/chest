import type { Dict } from '../../types';

// FORMAT REFERENCE ONLY — not part of the final merge (phase 3 ignores _example.ts files).
export const exampleMessages: Dict = {
  'example.hello': 'Hello',
  'example.greeting': 'Hello, {name}',
  'example.items': '{n} items'
};
