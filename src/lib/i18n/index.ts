export {
  getLocale,
  setLocale,
  initLocale,
  t,
  formatDate,
  formatNumber,
  plural
} from './store.svelte';

export { LOCALES, DEFAULT_LOCALE, LOCALE_LABELS, type Locale, type Dict } from './types';
